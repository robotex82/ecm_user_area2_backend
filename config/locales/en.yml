en:
  classes:
    ecm/user_area/backend/engine: Authentication
  activerecord:
    scopes:
      never_signed_in: Never signed in
  routes:
    mount:
      ecm_user_area_backend: '/backend/authentication'